#include <iostream>
#include <string>

using namespace std;

class Log
{
	private:
	string name;
	public:
	int state;
	
};
enum
{
	START,
	FIRST,
	SECOND,
	THIRD,
	END
}

void start(Log gm)
{
	if(playerReady == true)
	{
		gm.state = START;
	}
}

int main()
{
	Log game;
	start(game.state);
	return 0;
	
}