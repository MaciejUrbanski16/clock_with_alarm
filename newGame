#include <iostream>
#include <string>
#define CHANGE 25
using namespace std;


usigned int step = 0;
class Log
{
	private:
	string name;
	public:
	int state;
	
};
enum
{
	START,
	FIRST,
	SECOND,
	THIRD,
	END
}

void start(Log gm)
{
	if(playerReady == true)
	{
		gm.state = START;
	}
}

int main()
{
	Log game;
	start(game.state);
	return 0;
	
}
